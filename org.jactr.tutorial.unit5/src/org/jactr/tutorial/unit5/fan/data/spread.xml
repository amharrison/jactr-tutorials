<instrument window="0.1">   <!-- how often to poll -->
 <group id="Sentences">  <!-- everything in a group is rendered on the same graph -->
  <model pattern=".*">  <!-- what model to probe -->
   <chunk-type pattern="comprehend-sentence"> <!-- chunks of this type are to be probed -->
    <chunk pattern=".*">    <!-- select all of this chunk type regardless of name -->
     <probe pattern="SpreadingActivation" poll="true"/> <!-- what parameter(s) -->
    </chunk>
   </chunk-type>
  </model>
 </group>
</instrument>