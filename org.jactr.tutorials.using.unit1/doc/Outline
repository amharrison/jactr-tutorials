Environment.
 Project.
 Editor.
 Classpath
 
Execution. 
 - model recorder
   - gdiff of the two
 - associative viewer  
Replay.
 -  
Debug.
 - reconfigure layout

CleanUp after yourself.

- need to fix deferred add behavior so it actually works.
x all debug elements should defer behavior to the runtime.

Probing. <- display requires full refresh to paint first time?
Markers. <- aren't rendering point markers correctly (at all) ??